{
    "name": "wago-website",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "WebAssembly, made for Go.",
    "scripts": {
    "build": "npm run sync:check && npm run verify:corpora && tsc && rm -rf dist && mkdir -p dist && cp -r index.html assets corpora data v1 facts.md compatibility.md security.md benchmarks.md CNAME robots.txt sitemap.xml site.webmanifest llms.txt llms-full.txt dist/",
        "dev": "tsc --watch --preserveWatchOutput",
        "typecheck": "tsc --noEmit",
        "test:stats": "node --test scripts/stats-parsers.test.mjs",
        "verify:corpora": "node scripts/verify-corpora.mjs",
        "sync": "node scripts/sync-stats.mjs && node scripts/sync-schema.mjs && node scripts/sync-facts.mjs && node scripts/sync-ai-metadata.mjs",
        "sync:check": "node scripts/sync-stats.mjs --check && node scripts/sync-schema.mjs --check && node scripts/sync-facts.mjs --check && node scripts/sync-ai-metadata.mjs --check",
        "sync:facts": "node scripts/sync-facts.mjs",
        "sync:facts:check": "node scripts/sync-facts.mjs --check",
        "sync:ai": "node scripts/sync-ai-metadata.mjs",
        "sync:ai:check": "node scripts/sync-ai-metadata.mjs --check",
        "sync:schema": "node scripts/sync-schema.mjs",
        "sync:schema:check": "node scripts/sync-schema.mjs --check",
        "serve": "python3 -m http.server 8000"
    },
    "devDependencies": {
        "typescript": "^5.6.3"
    }
}

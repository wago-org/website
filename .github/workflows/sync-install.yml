name: Sync installers from wago

# Mirrors Wago's canonical bootstrap launchers independently, so unrelated
# generated-site failures cannot delay installer updates.

on:
  schedule:
    - cron: "47 7 * * *"
  workflow_dispatch:
  repository_dispatch:
    types: [wago-updated]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: sync-site
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.commit.outputs.changed }}
    steps:
      - name: Checkout site
        uses: actions/checkout@v4

      - name: Checkout wago (read-only)
        uses: actions/checkout@v4
        with:
          repository: wago-org/wago
          ref: main
          token: ${{ secrets.WAGO_RO_TOKEN }}
          path: wago-src
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Mirror canonical installers
        env:
          WAGO_DIR: ${{ github.workspace }}/wago-src
        run: npm run sync:install

      - name: Commit if changed
        id: commit
        run: |
          if git diff --quiet -- install.sh install.cmd; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "Installers are unchanged - nothing to deploy."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add install.sh install.cmd
          git commit -m "chore: sync installers from wago"
          git push
          echo "changed=true" >> "$GITHUB_OUTPUT"

  deploy:
    needs: sync
    if: needs.sync.outputs.changed == 'true'
    uses: ./.github/workflows/deploy.yml
    permissions:
      contents: read
      pages: write
      id-token: write
